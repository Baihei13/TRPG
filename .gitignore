# Dependencies / build tooling
node_modules/
.npm/

# Staging packs written while Foundry holds locks
packs-staging/

# LevelDB runtime only (numbered 000*.log may hold pack data — keep those)
packs/**/LOCK
packs/**/LOG
packs/**/LOG.old

# Leftover / scratch packs
packs/homebrew-items-new/

# Temp / local scratch
_tmp_unpack_test/
.DS_Store
Thumbs.db
*.bak
*.tmp

# Editor
.vscode/
.idea/
*.swp
